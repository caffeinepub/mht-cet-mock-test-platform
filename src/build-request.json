{
  "kind": "build_request",
  "title": "Add admin role verification popup on registration page",
  "projectName": "Concept Delta",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-133",
      "text": "Add an informational popup or alert banner in the AdminRegistration.tsx component that displays the message 'Only admin can assign user roles' when a user attempts to register an admin principal, explaining that admin registration requires existing admin authorization (except for the first admin setup)",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-last"
        ],
        "quotes": [
          "at the time of admin registration show pop up only admin can assign user role"
        ]
      },
      "acceptanceCriteria": [
        "A modal dialog or alert banner appears when the user clicks the 'Register as Admin' button",
        "The popup displays the exact text: 'Only admin can assign user roles'",
        "The popup clarifies that this is an admin-protected operation",
        "The popup appears before or after the registration attempt to inform users of the authorization requirement",
        "For the first admin registration (when no admins exist), the popup should also mention that the first registration is open for initial setup"
      ]
    },
    {
      "id": "REQ-134",
      "text": "Update the AdminRegistration.tsx component to display the 'Unauthorized: Only admins can assign user roles' error message more prominently as a modal popup or alert dialog instead of just showing it in a toast notification, making it clear to users that they lack the required admin permissions",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-last"
        ],
        "quotes": [
          "show pop up only admin can assign user role",
          "Registration Failed - Unauthorized: Only admins can assign user roles"
        ]
      },
      "acceptanceCriteria": [
        "When the backend returns an 'Unauthorized' error during admin registration, display it in a modal dialog or prominent alert",
        "The error popup includes the full error message explaining that only existing admins can assign admin roles",
        "The user can dismiss the popup with a close button or OK button",
        "The popup styling matches the existing UI design system",
        "For users who are already admins, clarify in the UI that they can successfully register other admins"
      ]
    }
  ],
  "constraints": [
    "Do not modify the backend authorization logic for admin registration",
    "Use existing UI component patterns from frontend/src/components/ui for modal/dialog implementation",
    "Maintain consistency with existing toast notification system"
  ],
  "nonGoals": [
    "Bypassing or disabling the admin authorization check",
    "Creating a separate registration flow for non-admin users",
    "Adding email notifications for failed registration attempts"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}